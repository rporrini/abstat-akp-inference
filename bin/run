#!/bin/sh

set -e

relative_path=`dirname $0`
this_directory=`cd $relative_path;pwd`

cd $this_directory

set +e
docker build --rm -t akp-inference .
docker rm -f $(docker ps -aq)
docker run -it --name akp-inference akp-inference "$@"

